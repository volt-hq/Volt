---
"@hansjm10/volt-coding-agent": patch
---

fix(remote): Corrected remote-access documentation to name the actual Iroh relay modes and `production` default, and documented the required workspace availability metadata for remote hosts. ([#97](https://github.com/volt-hq/Volt/issues/97))
