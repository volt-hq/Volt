---
"@hansjm10/volt-coding-agent": patch
---

fix(sessions): Fixed session store lock timeouts, full disks, and I/O failures being reported as rejected writes or corrupt sessions instead of busy, full, or unavailable storage.
