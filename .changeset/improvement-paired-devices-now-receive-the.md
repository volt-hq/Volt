---
"@hansjm10/volt-coding-agent": patch
---

improvement(remote): Paired devices now receive the host.manage.v1 capability by default, so capability advertisement, host-action responses, and keep-awake work without a custom access grant.
