---
"@hansjm10/volt-coding-agent": patch
---

fix(remote): Hosts now reconnect to the relay within seconds of a Volt Pro renewal instead of up to an hour later, and the daemon logs a subscription suspension once instead of on every retry.
