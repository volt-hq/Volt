---
"@hansjm10/volt-coding-agent": patch
---

improvement(rpc): RPC client authors can now validate integrations against the committed `packages/coding-agent/contract/rpc-schema.json` JSON Schema, which CI keeps aligned with the wire contract.
