---
"@hansjm10/volt-coding-agent": patch
---

fix(remote): Fixed Register current directory in /remote silently using a registered parent instead of registering the child directory. ([#418](https://github.com/volt-hq/Volt/issues/418))
