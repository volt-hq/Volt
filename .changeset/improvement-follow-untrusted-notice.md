---
"@hansjm10/volt-coding-agent": patch
---

improvement(subagents): Follow-mode results are prefixed with an untrusted-data notice, matching the hardening already applied to chain {previous} substitution and registry listings, since followed runs were prompted elsewhere in the session tree.
