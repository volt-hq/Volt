---
"@hansjm10/volt-coding-agent": patch
---

fix(subagents): Small aggregate output limits no longer truncate away the spawn confirmation token; only the registry listing competes for the preflight byte budget.
