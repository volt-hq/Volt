---
"@hansjm10/volt-coding-agent": patch
---

fix(tui): Explicit subagent registry list/follow calls and spawn failures that occur before any child is created now render in the conversation; only successful spawn preflights stay hidden.
