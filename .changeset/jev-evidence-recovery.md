---
"@hansjm10/volt-coding-agent": patch
---

improvement(extensions): Jev preparation resolves source references, verifies candidate paths, reads implementation windows from symbol locations, and retries transient Gateway failures within its existing request budget.
