# Standalone Binary Capabilities

The prebuilt Bun executable is a local Volt CLI/TUI distribution. It supports
local interactive and print-mode agent sessions.

It does **not** include the native `@number0/iroh` adapter and cannot run
`volt daemon` or provide remote/iOS access. Install
`@earendil-works/volt-coding-agent` through npm for daemon and remote support.
