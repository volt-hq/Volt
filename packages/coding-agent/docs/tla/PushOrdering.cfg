\* TLC configuration for PushOrdering.tla
\*   ./check.sh PushOrdering
\* Checks the register_push_target -> register_live_activity ordering and stale-
\* channel invalidation across the phone/daemon two-phase registration.

SPECIFICATION Spec

CONSTANTS
    Channels = {c1, c2}
    NONE = NONE

INVARIANT
    POTypeOK
    OrderingGate
    SendAfterConfirm
    StaleChannelInvalidated
    RegisteredLAHadStoredChannel
