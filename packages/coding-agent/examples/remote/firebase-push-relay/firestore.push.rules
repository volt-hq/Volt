rules_version = '2';

service cloud.firestore {
	match /databases/{database}/documents {
		match /voltPushTargets/{pushTargetId} {
			allow read, write: if false;
		}
		match /{document=**} {
			allow read, write: if false;
		}
	}
}
