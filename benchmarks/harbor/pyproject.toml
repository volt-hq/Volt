[project]
name = "volt-harbor-evals"
version = "0.1.0"
description = "Pinned Harbor integration and fixed-model pilot for Volt"
requires-python = ">=3.12,<3.15"
dependencies = [
  "harbor==0.13.2",
]

[tool.uv]
package = false
