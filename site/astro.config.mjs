import { defineConfig } from "astro/config";
import starlight from "@astrojs/starlight";
import { readFileSync } from "node:fs";

// Generated by scripts/sync-docs.mjs from packages/coding-agent/docs/docs.json
const sidebar = JSON.parse(readFileSync(new URL("./src/generated/sidebar.json", import.meta.url), "utf8"));
const redirects = JSON.parse(readFileSync(new URL("./src/generated/redirects.json", import.meta.url), "utf8"));

export default defineConfig({
  site: "https://volt-cli.dev",
  redirects,
  integrations: [
    starlight({
      title: "Volt",
      description: "A coding agent that runs on your machine and follows you from terminal to phone.",
      logo: { src: "./public/volt-icon.png", alt: "Volt" },
      favicon: "/volt-icon.png",
      social: [{ icon: "github", label: "GitHub", href: "https://github.com/volt-hq/Volt" }],
      sidebar,
      editLink: {
        baseUrl: "https://github.com/volt-hq/Volt/edit/main/packages/coding-agent/docs/",
      },
      customCss: ["./src/styles/starlight.css"],
      components: {},
    }),
  ],
});
